<div>
 
  {{#if product}}
    {{#each product}}
      {{>sellYourItems}}
    {{/each}}
    <script src="/javascript/sell.js"></script>
  {{else}}
    <h2>You have no products to sell</h2>
  {{/if}}
</div>